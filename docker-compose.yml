services:
  copick-server:
    build:
      context: .
      dockerfile: Dockerfile
      args: 
        - BUILDKIT_INLINE_CACHE=1
    platform: linux/amd64 # copick depends on numcodecs 0.15.1 which is not available for arm64
    volumes:
      - .:/app
    ports:
      - "8012:8012"
    command: ["uv", "run", "copick_server/server.py", "example_copick.json", "--host", "0.0.0.0", "--port", "8012"]
networks:
  default:
    name: copick-server
    driver: overlay
    attachable: true